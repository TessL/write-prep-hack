<h1>New Essay</h1>
<%= form_for :essay , url: essays_path do |f| %>

<% if @essay.errors.any? %>
  <div id = "error_explanation">
    <h2>
      <%= pluralize(@essay.errors.count, "error") %> prohibited this essay from being saved:
    </h2>
    <ul>
      <% @essay.errors.full_message.each do |msg| %>
      <li> <%= msg %> </li>
      <% end %>
    </ul>
  </div>
  <% end %>
  
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', essays_path %>
